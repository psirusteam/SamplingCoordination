% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Overlap_sample.R
\name{Overlap_sample}
\alias{Overlap_sample}
\title{Overlapped Sample Selection Between Two Consecutive Periods}
\usage{
Overlap_sample(
  psu_frame,
  strata,
  id_psu,
  n_h,
  overlap,
  prev_sample,
  method = "MAS",
  sort_var
)
}
\arguments{
\item{psu_frame}{A \code{data.frame} or \code{tibble} containing the PSU frame with the coordination scores already computed (output of \code{Generate_random_2()}).}

\item{strata}{Unquoted name of the stratum variable in \code{psu_frame}.}

\item{id_psu}{Unquoted name of the PSU identifier variable in \code{psu_frame}.}

\item{n_h}{A \code{data.frame} or \code{tibble} with two columns: the stratum variable (same name as in \code{psu_frame}) and \code{n_h} with the sample size per stratum.}

\item{overlap}{Numeric value between 0 and 1 indicating the desired overlap proportion (e.g., \code{0.38} = 38\%).}

\item{prev_sample}{Unquoted name of the dichotomous variable (1/0) in \code{psu_frame} indicating whether the PSU was selected in the previous period.}

\item{method}{Character string: one of \code{"MAS"}, \code{"Pareto"}, or \code{"Poisson"}. Used only for the summary message.}

\item{sort_var}{Unquoted name of the coordination score column in \code{psu_frame} (e.g., \code{Xi_Poisson}, \code{Xi_Pareto}, \code{Xi_Perman}).}
}
\value{
The input \code{psu_frame} with additional columns:
\describe{
  \item{\code{n_m}}{Number of PSUs retained from the previous period in the stratum.}
  \item{\code{n_u}}{Number of new PSUs selected in the stratum.}
  \item{\code{in_sample}}{Integer (1/0): 1 if the PSU is selected in the current period, 0 otherwise.}
  \item{\code{type}}{Character: \code{"retained"} for PSUs kept from the previous period, \code{"new"} for newly selected PSUs, \code{NA} for non-selected.}
}
}
\description{
Selects a coordinated sample between two consecutive periods guaranteeing a desired overlap rate. Within each stratum, PSUs from the previous period are first retained (up to the overlap target), then the remaining PSUs are selected from those not previously sampled.
}
\examples{
\dontrun{
# Step 1: generate coordination scores
frame <- Generate_random_2(
  data     = psu_frame,
  id_psu   = cod_estab,
  seed     = 2025,
  method   = "Poisson",
  size_var = n_students,
  strata   = strata
)

# Step 2: select overlapped sample
result <- Overlap_sample(
  psu_frame   = frame,
  strata      = strata,
  id_psu      = cod_estab,
  n_h         = n_h_df,
  overlap     = 0.38,
  prev_sample = in_sample_2025,
  method      = "Poisson",
  sort_var    = Xi_Poisson
)

# PSUs selected in current period
result %>% filter(in_sample == 1L)
}
}
