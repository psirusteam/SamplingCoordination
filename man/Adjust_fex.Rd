% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Adjust_fexl.R
\name{Adjust_fex}
\alias{Adjust_fex}
\title{Adjust expansion factors for household surveys}
\usage{
Adjust_fex(
  data,
  household_id,
  strata,
  disposition_code,
  major_domain,
  area,
  pi_second_stage,
  pi_first_stage,
  keep_steps = TRUE,
  step_by_step = TRUE,
  view_steps = TRUE
)
}
\arguments{
\item{data}{A data frame with household-level records.}

\item{household_id}{Unquoted column name with the unique household identifier.}

\item{strata}{Unquoted column name with the sampling stratum.}

\item{disposition_code}{Unquoted column name with fieldwork result category.
Valid categories are: \code{"ER"}, \code{"ENR"}, \code{"IN"}, and \code{"UNK"}.}

\item{major_domain}{Unquoted column name for major administrative division (DAM).}

\item{area}{Unquoted column name for geographic area.}

\item{pi_second_stage}{Unquoted column name with second-stage inclusion probability.}

\item{pi_first_stage}{Unquoted column name with first-stage inclusion probability.}

\item{keep_steps}{Logical. If \code{TRUE}, returns all intermediate datasets and the final dataset in a list. If \code{FALSE}, returns only the final dataset.}

\item{step_by_step}{Logical. If \code{TRUE}, pauses after each stage until pressing Enter.}

\item{view_steps}{Logical. If \code{TRUE}, opens each stage dataset with \code{View()} in interactive sessions.}
}
\value{
A data frame with adjusted weights for responding eligible households (\code{ER}) including:
\code{d_1k}, \code{a_b}, \code{d_2k}, \code{d_3k}, \code{I_k}, \code{D_k}, \code{phi_k}, and \code{d_4k}.
If \code{keep_steps = TRUE}, returns a list with \code{step_a}, \code{step_b}, \code{step_c}, \code{step_d}, \code{summary_a}, \code{summary_b}, \code{summary_c}, \code{summary_d}, and \code{base_ER_final}.
}
\description{
Applies the standard workflow to adjust household expansion factors (FEX):
design weight, adjustment for unknown eligibility, exclusion of ineligible units,
and nonresponse adjustment through a logistic response-propensity model.
}
\examples{
toy_data <- data.frame(
  hh_id = 1:8,
  stratum = c("S1", "S1", "S1", "S1", "S2", "S2", "S2", "S2"),
  result = c("ER", "ENR", "UNK", "IN", "ER", "ENR", "ER", "UNK"),
  dam = c("D1", "D1", "D1", "D1", "D2", "D2", "D2", "D2"),
  area = c("U", "R", "U", "R", "U", "R", "U", "R"),
  pi2 = rep(0.5, 8),
  pi1 = rep(0.5, 8)
)

Adjust_fex_cepal(
  data = toy_data,
  household_id = hh_id,
  strata = stratum,
  disposition_code = result,
  major_domain = dam,
  area = area,
  pi_second_stage = pi2,
  pi_first_stage = pi1,
  keep_steps = FALSE,
  step_by_step = TRUE,
  view_steps = TRUE
)
}
\author{
Yury Vanessa Ochoa
}
