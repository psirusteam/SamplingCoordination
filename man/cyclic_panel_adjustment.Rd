% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cyclic_panel_adjustment.R
\name{cyclic_panel_adjustment}
\alias{cyclic_panel_adjustment}
\title{Cyclic adjustment to complete incomplete or missing mini-panels}
\usage{
cyclic_panel_adjustment(
  assigned_frame,
  demand_table,
  panels_by_scheme,
  scheme_column = "scheme",
  geo_column = "geo",
  stratum_column = "stratum",
  PSU_column = "PSU",
  order_column = "rank"
)
}
\arguments{
\item{assigned_frame}{data.frame output of \code{assign_PSUs_to_panels()},
with columns \code{geo_column}, \code{stratum_column}, \code{PSU_column},
\code{order_column}, \code{scheme_column}, and \code{panel}.}

\item{demand_table}{data.frame with columns \code{geo_column},
\code{stratum_column}, \code{panel_letter}, and \code{n_assigned}.}

\item{panels_by_scheme}{Named list mapping each scheme to its theoretical
panel vector. E.g.:
\code{list("12_minipaneles" = mp_teoricos_12, "4_minipaneles" = mp_teoricos_4)}.}

\item{scheme_column}{Character. Name of the scheme column. Default \code{"scheme"}.}

\item{geo_column}{Character. Name of the geo stratum column. Default \code{"geo"}.}

\item{stratum_column}{Character. Name of the NSE stratum column. Default \code{"stratum"}.}

\item{PSU_column}{Character. Name of the PSU identifier column. Default \code{"PSU"}.}

\item{order_column}{Character. Name of the ordering column. Default \code{"rank"}.}
}
\value{
A tibble with columns \code{geo_column}, \code{PSU_column},
  \code{stratum_column}, and \code{panel_cyclic} containing only the
  PSUs added by the cyclic adjustment.
}
\description{
After the initial PSU assignment, some mini-panels may be incomplete
(fewer PSUs than demanded) or entirely absent. This function completes
incomplete panels first, then fills missing panels, taking PSUs cyclically
by stratum (ordered by \code{order_column}) without resetting the index
between panels.
}
\author{
Yury Vanessa Ochoa
}
